<!DOCTYPE html>
<html>
<head>
  <title>MultiTenantCMS</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="navbar navbar-default navbar-static-top">
    <div class="container">
      <div class="navbar-header">
        <%= link_to @site.title, @site, class: "navbar-brand" %></a>
      </div>

      <ul class="nav navbar-nav">
        <%= content_tag_for :li, @site.pages.roots do |page| %>
          <%= link_to page.title, [@site, page] %>
        <% end %>
      </ul>
    </div>
  </nav>

  <div class="container">
    <ol class="breadcrumb">
      <%= render_breadcrumbs :tag => :li, :separator => "" %>
    </ol>
    <div class="row">
      <div class="col-xs-3">
        <div class="list-group">
          <%= link_to "页面管理", site_settings_pages_path(@site), class: "list-group-item" %>
          <%= link_to "文章管理", site_settings_posts_path(@site), class: "list-group-item" %>
          <%= link_to "评论管理", site_settings_comments_path(@site), class: "list-group-item" %>
          <%= link_to "链接管理", site_settings_links_path(@site), class: "list-group-item" %>
          <%= link_to "文章分类", site_settings_categories_path(@site), class: "list-group-item" %>
        </div>
      </div>
      <div class="col-xs-9">
        <%= yield %>
      </div>
    </div>
  </div>

</body>
</html>

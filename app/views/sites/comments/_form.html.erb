<%= simple_form_for [@site, @post, @post.comments.new] do |f| %>
  <div class="panel panel-default">
    <div class="panel-body">
      <%= f.input :content %>
    </div>
    <div class="panel-footer">
      <%= f.button :submit %>
    </div>
  </div>
<% end %>

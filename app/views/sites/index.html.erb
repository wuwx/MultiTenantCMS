<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Title</th>
    </tr>
  </thead>
  <tbody>
    <%= content_tag_for :tr, @sites do |site| %>
      <td><%= link_to site.name, site %></td>
      <td><%= link_to site.title, site %></td>
    <% end %>
  </tbody>
</table>

<%= simple_form_for [:settings, @site] do |f| %>
  <%= f.input :title %>
  <%= f.input :slug %>
  <%= f.input :theme, collection: ["default", "flat-ui", "bootstrap-material-design"] %>
  <%= f.button :submit %>
<% end %>
